I_ADD 	 scitani (a,b,vysledek)
I_SUB 	 odcitani (a,b,vysledek)
I_MUL 	 nasobeni (a,b,vysledek)
I_DIV 	 deleni (a,b,vysledek)

I_CMP 	 porovnani (a,b,vysledek) -  a>b ->vysledek=1
				   					 a<b ->vysledek=-1
				   					 a=b ->vysledek=0

I_READ	 cteni (datovy typ, x, kam)
I_WRITE  zapis (datovy typ, x, odkud)
I_STOP	 konec programu (x,x,x)
I_LABEL  navesti (x,x,x)

I_CALL	 volani funkce (,x,navratova hodnota)
I_FRAMEC vytvoreni ramce fce (id funkce,x,x)
I_PUSHP  ulozeni argumentu volani funkce ()
I_FRAMED zruseni ramce aktivni fce (,,)
I_RETURN return (co,x,kam)

I_BLOCKC alokovani bloku pro promennou (lok/glob, index, datovy typ)
I_ASSIGN prirazeni hodnoty do promenne (lok/glob, datovy typ, kam prirazuju, co prirazuju) //jeste nejsou jasny parametry
I_GOTO 	 skok (podminka,x,adresa)

//generovani instrukci
(I_BLOCKC, datovy typ, NULL, lok/glob, index, 0);
(I_ASSIGN, datovy typ, pripadnej string, lok/glob, index odkud, index kam);
(I_ADD, datovy typ, lok/glob, index1, index2, index kam);
(I_FRAMEC, 0, id funkce, 0,0,0);
(I_PUSHP, datovy typ, NULL, lok/glob, index odkud, 0);
(I_CALL, dt neni potreba, ukazatel na pasku fce, index promenne kam priradime, 
			je to prirazeni do lok/glob promenne, <0 = void...1 = lok...2 = glob>
			je to vestavena funkce?? 1=ANO);
(I_RETURN, dt neni potreba, NULL, budu vracet ano1/ne0, 0, kde je vysledek co budu vracet);